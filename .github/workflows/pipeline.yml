name: CI/CD pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Build Docker image
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: username/repository-name:latest
        
    - name: Run Docker container
      run: |
        docker pull cjbbt12/fulhaus_223:latest
        docker stop fulhaus_223 || true
        docker rm fulhaus_223 || true
        docker run -d --name fulhaus_223 -p 5000:5000 cjbbt12/fulhaus_223:latest